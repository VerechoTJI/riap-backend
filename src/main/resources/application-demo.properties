# Demo profile: H2 in-memory, no PostgreSQL needed
server.port=8080

app.datasource.url=jdbc:h2:mem:riapdb;DB_CLOSE_DELAY=-1;NON_KEYWORDS=VALUE;MODE=PostgreSQL
app.datasource.username=sa
app.datasource.password=

app.datasource.ddl-auto=create-drop
